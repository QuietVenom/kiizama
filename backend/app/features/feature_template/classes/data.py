from __future__ import annotations

from dataclasses import dataclass


@dataclass(slots=True)
class ExampleEntity:
    """Replace with dataclasses that represent your domain objects."""

    name: str = ""
